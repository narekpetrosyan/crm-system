import React from 'react';
import { observer } from 'mobx-react-lite';
import InnerLayout from '@layouts/InnerLayout/InnerLayout';

const EditWorker = observer(() => {
  return (
    <InnerLayout>
      <div>dsjkbndfjbndfbikdf</div>
    </InnerLayout>
  );
});

export default EditWorker;
